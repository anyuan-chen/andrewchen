import styled from "styled-components";
import { color, fontSize, fontStyle } from "styled-system";

const Title = styled.div`
  display: flex;
  width: 100%;
  padding-bottom: 6rem;
  ${fontSize}
  ${color}
  ${fontStyle}
`;

export default Title;
